# alpha-loot
Build a simple Node.js/TypeScript script that places a limit order using the @alpha-arcade/sdk package. Start by getting one order confirmed on-chain. The long-term goal is a winning strategy that earns LP rewards on Alpha Arcade.
